##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrules that are useful

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}

# ---------------------------------------------------------------------- #

layerrule = match:namespace swaync-control-center, blur on
layerrule = match:namespace swaync-control-center, ignore_alpha 0.5

#windowrule = opacity 0.9 0.6, match:class ^(rofi)$

layerrule = match:namespace swaync-notification-window, blur on
layerrule = match:namespace swaync-notification-window, ignore_alpha 0.1

# ----- GNS3 ----- #
workspace = special:magic, gapsout:50, gapsin:7

windowrule { # for GNS3 GUI
  name = GNS3
  match:class = ^(gns3)$
  #size = (monitor_w*0.95) (monitor_h*0.95)
  #size = (window_w*0.95) (window_h*0.95)
  #center = on
  workspace = special:magic
  animation = popin
}

windowrule { # for terminals
  name = kitty
  match:class = kitty
  float = on
  center = on
  #size = (monitor_w*0.9) (monitor_h*0.9)
  size = (monitor_w*0.5) (monitor_h*0.5)
  match:workspace = special:magic
}

windowrule { # for wireshark
  name = wireshark float in GNS3 labs
  match:class = org.wireshark.Wireshark
  float = on
  center = on
  #size = (monitor_w*0.9) (monitor_h*0.9)
  size = (monitor_w*0.5) (monitor_h*0.5)
  match:workspace = special:magic
}

windowrule { # for VMs
    name = floating VMs in GNS3
    match:class = Vncviewer
    float = on
    center = on
}

# ----- OBSIDIAN ----- #
workspace = special:obsidian, gapsout:50, gapsin:7

windowrule {
    name = obsidain
    match:class = ^(obsidian)$
    #size = (monitor_w*0.95) (monitor_h*0.95)
    #size = (window_w*0.95) (window_h*0.95)
    #center = on
    workspace = special:magic
    animation = popin
}

# ----- File picker for browser ----- #
windowrule {
    name = file picker in browser
    match:class = xdg-desktop-portal-gtk
    float = on
    center = on
    size = (monitor_w*0.8) (monitor_h*0.8)
}

# ----- Nautilus ----- #
windowrule {
    name = nautilus
    match:class = org.gnome.Nautilus
    float = on
    center = on
    size = (monitor_w*0.8) (monitor_h*0.8)
}

# ----- Captura -----#
layerrule = match:namespace hyprshot, no_anim on
layerrule = match:namespace selection, no_anim on

layerrule = match:namespace swappy, no_anim on

# ----- Zoomer -----#
windowrule {
    name = zoomer-wayland
    match:title = ^(wayland-boomer)$
    no_anim = true
    fullscreen = true
}

# ----- Sowon -----#
windowrule {
    name = sowon
    match:title = ^(.*sowon.*)$
    #fullscreen = true
}

# ----- Satty -----#
windowrule { # for terminals
  name = satty
  match:class = com.gabm.satty
  float = on
  center = on
  #size = (monitor_w*0.9) (monitor_h*0.9)
  #size = (monitor_w*0.5) (monitor_h*0.5)
}

# PUTTY
#windowrule {
#    name = putty
#    match:class = ^(putty)$
#    float = on
#    size = (monitor_w*0.8) (monitor_h*0.8)
#    center = on
#    opacity = 1.0 0.8
#    no_blur = on
#    workspace = special:magic
#}

# kitty
#windowrule {
#    name = kitty
#    match:class = kitty
#    float = on
#    center = on
#    #size = (monitor_w*0.9) (monitor_h*0.9)
#    size = (monitor_w*0.9) (monitor_h*0.9)
#}


# Ref https://wiki.hypr.land/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule {
#     name = no-gaps-wtv1
#     match:float = false
#     match:workspace = w[tv1]
#
#     border_size = 0
#     rounding = 0
# }
#
# windowrule {
#     name = no-gaps-f1
#     match:float = false
#     match:workspace = f[1]
#
#     border_size = 0
#     rounding = 0
# }
